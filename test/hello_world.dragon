[ entry point ]
dragon.main()() = {
    [ initialization message ]
    dragon.set("The dragon slumbers...%0a;")(message)
    dragon.print.buffer_as_string(message)()

    [ test compiler ]
    dragon.set("{{{}}}")(text)
    tools.json.compile(text)()
}
